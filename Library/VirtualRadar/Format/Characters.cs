namespace VirtualRadar.Format
{
    /// <summary>
    /// Aliases for characters that might be a bit confusing if we just had them in source.
    /// </summary>
    public static class Characters
    {
        /// <summary>
        /// The non-breaking space character.
        /// </summary>
        public const char NonBreakingSpace = ' ';   // ALT+160 :: 0xc2a0 in UTF8
    }
}
