# VirtualRadar.TileServer

Note that the `Settings` suffix is used to retain backwards compatability
with the VRS SDM web site. None of these classes are anything to do with
the `ISettings` local configuration bits.
